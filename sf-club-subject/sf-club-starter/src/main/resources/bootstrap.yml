spring:
  application:
    name: sf-club-subject
  profiles:
      active: dev
  cloud:
    nacos:
      config:
        server-addr: ip:port
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: ip:port